<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 平均診察時間
    |--------------------------------------------------------------------------
    |
    | 待合室での予想待ち時間計算に使用される、1患者あたりの平均診察時間（分）
    |
    */
    'average_exam_minutes' => env('CLINIC_AVERAGE_EXAM_MINUTES', 10),

    /*
    |--------------------------------------------------------------------------
    | 再呼出最大回数
    |--------------------------------------------------------------------------
    |
    | 患者が呼出に応答しない場合の最大再呼出回数
    |
    */
    'max_recall_count' => env('CLINIC_MAX_RECALL_COUNT', 3),

    /*
    |--------------------------------------------------------------------------
    | UIロックタイムアウト
    |--------------------------------------------------------------------------
    |
    | UI画面のロックが自動解除されるまでの時間（分）
    |
    */
    'ui_lock_timeout_minutes' => env('CLINIC_UI_LOCK_TIMEOUT', 10),

    /*
    |--------------------------------------------------------------------------
    | 診察なし会計の理由選択肢
    |--------------------------------------------------------------------------
    |
    | S2 → S7（診察なし会計）の際に選択可能な理由
    |
    */
    'no_exam_reasons' => [
        '処方箋のみ',
        '検査結果説明のみ',
        '予約変更',
        '会計のみ',
        'その他',
    ],

    /*
    |--------------------------------------------------------------------------
    | 受付コード形式
    |--------------------------------------------------------------------------
    |
    | QR/ICカードのコード形式検証用正規表現
    |
    */
    'visit_code_pattern' => env('CLINIC_VISIT_CODE_PATTERN', '/^[A-Z0-9]{6,20}$/'),

    /*
    |--------------------------------------------------------------------------
    | データ保持期間
    |--------------------------------------------------------------------------
    |
    | 完了済み来院データの保持期間（日数）
    |
    */
    'data_retention_days' => env('CLINIC_DATA_RETENTION_DAYS', 365),
];
